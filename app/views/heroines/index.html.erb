<h2>All Heroines</h2>

 <p> <%= form_tag(heroines_path, method: "get") do %>
 Search by Power!: <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag("Search") %>
<% end %> </p>



<ul>
  <% @heroines.each do |heroine| %>
    <li>
      <%= link_to heroine.super_name, heroine %>
    </li>
    <br>
  <% end %>
</ul>

<h3> <%= link_to "Create New Heroine!", new_heroine_path %> </h3>
